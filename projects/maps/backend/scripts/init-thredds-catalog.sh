#!/bin/bash
# Thredds catalog initialization is intentionally disabled.
#
# This script is kept in the repository (commented/disabled instead of deleted)
# to preserve project history and make rollback easy if needed.
