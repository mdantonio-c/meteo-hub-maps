---
project:
  title: MeteoHub Maps
  description: "Static maps for the MeteoHub project"
  version: "0.2"
  rapydo: "2.0"

variables:
  env:
    ACTIVATE_AUTH: 0
    FRONTEND_FRAMEWORK: nofrontend
    CORS_ALLOWED_ORIGIN: "*"
    SET_MAX_REQUESTS_PER_SECOND_API: 3000
    # ACTIVATE_REDIS: 1
